export interface Props {
  labelText: string;
  bodyText: string;
  linkText: string;
  href: string;
  icon: boolean;
  bodies: { href?: string; linkText?: string; bodyText: string }[];
}
export interface Data {}
export interface Computed {}
export interface Methods {}
