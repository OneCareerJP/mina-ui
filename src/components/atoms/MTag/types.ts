export interface Props {
  type: string;
  label: string;
  status: string;
  size: string;
  active: boolean;
  bold: boolean;
}
export interface Data {}
export interface Computed {}
export interface Methods {}
